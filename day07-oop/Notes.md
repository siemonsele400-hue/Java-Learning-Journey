### 1\. 概念篇：面向对象与内存原理

**对应视频：** `01-04` (快速入门、深刻认识、执行原理、注意事项)

#### 📝 深度分析

  * **核心思想**：以前写代码像“记流水账”（第一步干啥，第二步干啥）；现在写代码像“造物主”（先创造一个对象，然后指挥对象去干活）。
  * **类 (Class)**：是**设计图**。比如“手机图纸”。
  * **对象 (Object)**：是**实物**。比如“你手里那台具体的iPhone 16”。
  * **内存原理 (重点)**：
      * **栈 (Stack)**：存所有的局部变量。比如 `Phone p`，这个 `p` 只是一个遥控器（引用），存的是地址。
      * **堆 (Heap)**：存真正的对象实体。`new Phone()` 产生的东西都在这里。

#### 💻 代码演示

如何定义类，并将其实例化：

```java
// 1. 定义一个类 (图纸)
public class Phone {
    // 成员变量 (属性)
    String brand; // 品牌
    double price; // 价格

    // 成员方法 (行为)
    public void call() {
        System.out.println("手机正在打电话...");
    }
}

// 2. 测试类 (使用图纸造对象)
public class Test {
    public static void main(String[] args) {
        // 创建对象：类名 对象名 = new 类名();
        Phone p = new Phone(); 
        
        // 使用对象
        p.brand = "小米"; // 访问属性
        p.price = 1999.0;
        
        System.out.println(p.brand); // 输出：小米
        p.call(); // 调用方法
    }
}
```

-----

### 2\. 难点篇：`this` 关键字

**对应视频：** `05_this关键字.mp4`

#### 📝 深度分析

  * **为什么需要它？** 当方法的局部变量（形参）和成员变量**同名**时，Java遵循“就近原则”，会优先使用局部变量。
  * **`this` 是什么？** `this` 代表**当前调用这个方法的对象**的内存地址。谁调用这个方法，`this` 就是谁。

#### 💻 代码演示

注意看 `setName` 方法中 `this` 的作用：

```java
public class Student {
    String name; // 成员变量

    // 这里的 name 是形参 (局部变量)
    public void setName(String name) {
        // 错误写法：name = name; (这是自己赋值给自己，成员变量没变)
        
        // 正确写法：this.name 指代上面那个成员变量
        this.name = name; 
    }
    
    public void printThis() {
        System.out.println(this); // 打印出来是一个地址值
    }
}
```

-----

### 3\. 初始化篇：构造器 (Constructor)

**对应视频：** `06_构造器.mp4`

#### 📝 深度分析

  * **本质**：构造器是一种特殊的方法，名字必须和类名完全一致，且没有返回值类型（连void都不写）。
  * **作用**：在 `new` 对象的时候，**立刻**执行，用于给对象的属性赋初始值。
  * **大坑预警**：如果你写了一个有参数的构造器，Java就不再赠送那个默认的无参构造器了。**建议永远手动把无参构造器写上**，这被称为“标准写法”。

#### 💻 代码演示

```java
public class Car {
    String color;
    
    // 1. 无参构造器 (必须手动写上，习惯即标准)
    public Car() {
        System.out.println("无参构造器被触发了");
    }
    
    // 2. 有参构造器 (用于初始化)
    public Car(String color) {
        System.out.println("有参构造器被触发了");
        this.color = color;
    }
}

// 测试
// Car c1 = new Car();        // 触发无参
// Car c2 = new Car("红色");   // 触发有参，c2.color直接就是红色
```

-----

### 4\. 规范篇：封装与标准 JavaBean

**对应视频：** `07_封装.mp4`, `08_实体类.mp4`

#### 📝 深度分析

  * **封装的思想**：我的钱（变量），你不能直接伸手拿（`p.money = -100` 是不安全的）。你必须通过我提供的窗口（方法）来存取，我可以在窗口里检查你的钱合不合法。
  * **JavaBean**：这是Java开发中最常用的代码模板，以后写数据库表对应的类，全是这个格式。
    1.  成员变量全部 `private`。
    2.  提供无参、全参构造器。
    3.  提供 `getter` (取值) 和 `setter` (赋值) 方法。

#### 💻 代码演示 (请熟练背诵/默写此结构)

```java
public class User {
    // 1. 私有化成员变量
    private String username;
    private int age;

    // 2. 无参构造器
    public User() {
    }

    // 3. 全参构造器
    public User(String username, int age) {
        this.username = username;
        this.age = age;
    }

    // 4. Getter 和 Setter 方法
    public String getUsername() {
        return username;
    }

    public void setUsername(String username) {
        this.username = username;
    }

    public int getAge() {
        return age;
    }

    public void setAge(int age) {
        // 封装的好处：逻辑拦截
        if (age >= 0 && age <= 120) {
            this.age = age;
        } else {
            System.out.println("年龄不合法");
        }
    }
}
```

-----

### 5\. 实战篇：综合案例

**对应视频：** `09_综合案例.mp4`

#### 📝 深度分析

  * **场景**：通常是让你模拟“购物车”或者“电影信息展示”。
  * **核心技术点**：**对象数组**。
      * 基本数据类型数组：`int[] arr = {1, 2, 3};`
      * 引用数据类型数组：`Movie[] movies = new Movie[3];`
  * **难点**：数组里存的不是电影本身，而是电影对象的**地址**。

#### 💻 代码演示 (模拟电影信息展示)

```java
// 假设上面已经写好了一个标准的 Movie 类 (含 title, score 属性)

public class MovieTest {
    public static void main(String[] args) {
        // 1. 定义数组，用来存3部电影对象
        Movie[] movies = new Movie[3];
        
        // 2. 创建电影对象，并存入数组
        movies[0] = new Movie("战狼", 9.7);
        movies[1] = new Movie("哪吒", 9.6);
        movies[2] = new Movie("流浪地球", 9.5);
        
        // 3. 遍历数组，展示信息
        // 这里的 m 就是数组里拿出来的每一个 Movie 对象
        for (int i = 0; i < movies.length; i++) {
            Movie m = movies[i]; 
            System.out.println("电影名：" + m.getTitle());
            System.out.println("评分：" + m.getScore());
            System.out.println("-----------------");
        }
    }
}
```

-----

### 🚀 总结与下一步

这一天的视频其实是在教你\*\*“立规矩”\*\*：

1.  变量要私有 (`private`)。
2.  方法要公开 (`public`)。
3.  初始化用构造器。
4.  自己人（本类）用 `this` 区分名字。
