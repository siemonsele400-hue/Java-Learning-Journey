Day 03 - Java 流程控制 📝
> 学习目标：深入理解程序的执行流程，从基础的逻辑判断（if/switch）到复杂的循环控制（for/while/do-while），再到循环嵌套与随机数生成。掌握这些，你的代码就有了“思考”的能力。
> 
目录导航
 * 程序流程控制与 If 分支 
 * Switch 分支结构 
 * For 循环结构 
 * While 循环结构 
 * Do-While 循环与死循环 
 * 循环嵌套 
 * 跳转控制语句 
 * Random 随机数 
1. 程序流程控制与 If 分支
程序默认是顺序结构（从上往下依次执行）。为了加入逻辑判断，我们需要分支结构。  
If 语句的三种格式  
 * 单分支：if (关系表达式) { 语句体; }
 * 双分支：if (关系表达式) { ... } else { ... }
 * 多分支：if (关系表达式1) { ... } else if (关系表达式2) { ... } else { ... }  
💻 代码案例：考试奖励 (多分支 IF)
```java
import java.util.Scanner; // 导包

public class IfDemo {
    public static void main(String[] args) {
        // 1. 创建键盘录入对象
        Scanner sc = new Scanner(System.in); // 准备录入数据
        
        System.out.println("请输入小明的考试成绩："); // 提示用户
        int score = sc.nextInt(); // 接收整数成绩

        // 2. 对成绩进行合法性判断 (0-100)
        if (score >= 0 && score <= 100) { // 成绩必须在0到100之间
            // 3. 多分支判断奖励
            if (score >= 95) { // 如果成绩大于等于95
                System.out.println("奖励：自行车一辆"); // 顶级奖励
            } else if (score >= 85) { // 如果成绩在 85-94 之间
                System.out.println("奖励：变形金刚玩具"); // 次级奖励
            } else if (score >= 80) { // 如果成绩在 80-84 之间
                System.out.println("奖励：游乐场玩一次"); // 普通奖励
            } else { // 如果成绩小于 80
                System.out.println("奖励：混合双打套餐"); // 惩罚
            }
        } else {
            System.out.println("成绩输入错误！"); // 处理非法输入
        }
    }
}
```
2. Switch 分支结构
switch 适用于单值匹配，代码结构比一长串 if-else 更清晰。  
 穿透性 (Case Fall-through)
 * 概念：如果 case 中没有写 break，程序会直接向下执行后续 case 的代码，直到遇到 break 或结束。
 * 应用：当多个 case 的处理逻辑完全一致时（比如周一到周五都是“工作日”）。  
💻 代码案例：Switch 穿透
```java
public class SwitchDemo {
    public static void main(String[] args) {
        int week = 2; // 定义星期几

        switch (week) { // 匹配变量 week
            case 1: // 星期一
            case 2: // 星期二
            case 3: // 星期三
            case 4: // 星期四
            case 5: // 星期五
                // 1-5 都没写 break，都会利用穿透性执行这一句
                System.out.println("工作日：努力写代码"); // 统一处理逻辑
                break; // 遇到 break，跳出 switch
            case 6: // 星期六
            case 7: // 星期日
                System.out.println("休息日：睡懒觉"); // 统一处理逻辑
                break; // 结束
            default: // 非法输入
                System.out.println("输入有误"); // 默认处理
                break; 
        }
    }
}
```
3. For 循环结构
最常用的循环，适用于循环次数已知的场景。  
💻 代码案例：水仙花数 (重点案例)
> 需求：找出 100-999 之间的所有水仙花数（个位³+十位³+百位³ == 原数）。
>
```java
public class ForNarcissistic {
    public static void main(String[] args) {
        int count = 0; // 定义变量记录个数

        // 1. 遍历所有三位数
        for (int i = 100; i <= 999; i++) { // i 从 100 开始，到 999 结束
            
            // 2. 拆分个、十、百位
            int ge = i % 10;        // 个位
            int shi = i / 10 % 10;  // 十位
            int bai = i / 100;      // 百位

            // 3. 判断是否满足水仙花数规则
            // Math.pow(a, 3) 也可以，但直接相乘更直观
            if ((ge*ge*ge + shi*shi*shi + bai*bai*bai) == i) { 
                System.out.print(i + " "); // 输出该数字
                count++; // 计数器+1
            }
        }
        System.out.println("\n总个数：" + count); // 循环结束后输出总数
    }
}
```
4. While 循环结构
适用于循环次数不确定，只知道终止条件的场景。  
💻 代码案例：珠穆朗玛峰折纸 (重点案例)
> 需求：纸张厚度 0.1mm，折叠多少次能达到珠峰高度 8848860mm？
>
```java
public class WhileEverest {
    public static void main(String[] args) {
        double peakHeight = 8848860; // 珠峰高度
        double paper = 0.1;          // 纸张初始厚度
        int count = 0;               // 折叠次数

        // 1. 当纸张厚度 小于 珠峰高度时，继续折叠
        while (paper < peakHeight) { 
            paper *= 2; // 折叠一次，厚度翻倍
            count++;    // 次数加一
        }

        // 2. 输出结果
        System.out.println("需要折叠：" + count + "次"); // 输出次数
        System.out.println("最终厚度：" + paper);       // 输出最终厚度
    }
}
```
5. Do-While 循环与死循环  
5.1 Do-While 循环
 * 格式：do { ... } while(条件);
 * 特点：先执行，后判断。无论条件是否满足，循环体至少执行一次。  
5.2 死循环 (Dead Loop)
程序一直执行，无法自动停止。通常配合 break 使用。  
// 最常用的死循环写法
while (true) {
    // 这里的代码会一直执行
    // 通常内部会有 if (条件) break; 来控制退出
}

6. 循环嵌套
概念：一个循环的大括号内包含另一个循环（大圈套小圈）。
执行流程：外层循环执行 1 次，内层循环执行 1 圈。  
💻 代码案例：打印星号矩阵
```java
public class LoopNested {
    public static void main(String[] args) {
        // 外层循环：控制打印多少行 (例如 4 行)
        for (int i = 0; i < 4; i++) { 
            // 内层循环：控制每行打印多少个星星 (例如 5 个)
            for (int j = 0; j < 5; j++) { 
                System.out.print("*"); // 打印星星，不换行
            }
            // 内层循环结束后，说明这一行打印完了，需要换行
            System.out.println(); // 换行
        }
    }
}
```
8. 跳转控制语句
 * break：结束/终止当前整个循环（或 switch）。
 * continue：跳过本次循环体中剩余代码，直接进入下一次循环。
```java
<!-- end list -->
for (int i = 1; i <= 5; i++) {
    if (i == 3) {
        continue; // 如果是3，跳过打印，直接去 i++ 变成 4
        // break; // 如果是 break，直接结束整个循环，4和5都不会打印
    }
    System.out.println(i);
}
// continue输出：1, 2, 4, 5
// break输出：1, 2
```
9. Random 随机数
Java 提供的用于生成伪随机数的类。  
基础使用
 * 导包：import java.util.Random;
 * 创建对象：Random r = new Random();
 * 生成数据：int num = r.nextInt(10); (生成 0 到 9)  
💻 代码案例：猜数字小游戏 (综合练习)
```java
import java.util.Random; // 导入Random
import java.util.Scanner; // 导入Scanner

public class RandomGuess {
    public static void main(String[] args) {
        // 1. 生成 1-100 的随机数
        Random r = new Random(); // 创建 Random 对象
        int luckyNum = r.nextInt(100) + 1; // 生成 0-99，加1后变为 1-100

        Scanner sc = new Scanner(System.in); // 创建 Scanner 对象

        // 2. 循环猜数字，因为不知道要猜多少次，使用 while(true)
        while (true) {
            System.out.println("请输入你猜的数字(1-100)："); // 提示
            int guessNum = sc.nextInt(); // 录入用户猜测

            // 3. 比较判断
            if (guessNum > luckyNum) { // 猜大了
                System.out.println("猜大了！"); // 提示
            } else if (guessNum < luckyNum) { // 猜小了
                System.out.println("猜小了！"); // 提示
            } else { // 猜中了
                System.out.println("恭喜你，猜中了！"); // 成功提示
                break; // 核心：猜中后必须跳出死循环
            }
        }
    }
}

```
