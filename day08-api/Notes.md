### 第一部分：API 与 字符串基础 (视频 01-05)

#### 1\. 什么是 API 和 包 (Video 01-02)

  * **概念**：API (Application Programming Interface) 就是 Java 官方写好的现成代码。你不用管它是怎么写的，只要知道怎么用。
  * **包 (Package)**：
      * **作用**：用来管理类文件的（就像电脑文件夹分类）。
      * **核心规则**：
          * 同一个包下的类，互相可以直接使用。
          * 不同包下的类，必须使用 `import` 导包。
          * **特权**：`java.lang` 包下的类（如 `String`, `System`, `Math`）不需要导包，直接用。

#### 2\. String 的内存原理 (Video 03, 05) **(重难点)**

这是面试必问的知识点，必须理解“字符串常量池”。

  * **特性**：String 是**不可变**的。一旦创建，内容永远不会变。如果你给字符串变量重新赋值，它只是指向了一个新的地址，原来的那个字符串还在内存里（直到被回收）。

  * **两种创建方式的区别**：

    1.  `String s = "abc";`
          * **原理**：Java 会先去堆内存里的 **String Constant Pool (字符串常量池)** 找有没有 "abc"。如果有，直接复用地址；如果没有，创建一个新的。
          * **优势**：节省内存。
    2.  `String s = new String("abc");`
          * **原理**：不管常量池里有没有，强制在堆内存中开辟一块**新空间**。
          * **劣势**：浪费内存，且地址一定不同。

  * **代码验证 (必背)**：

    ```java
    String s1 = "abc";
    String s2 = "abc";
    String s3 = new String("abc");

    System.out.println(s1 == s2); // true (都指向常量池的同一个地址)
    System.out.println(s1 == s3); // false (s3是堆里的新地址)
    ```

#### 3\. String 的常用方法 (Video 04)

这些方法不需要死记硬背，多敲几次就记住了。

| 方法名 | 说明 | 示例代码 | 结果 |
| :--- | :--- | :--- | :--- |
| `length()` | 获取长度 | `"abc".length()` | `3` |
| `charAt(int index)` | 获取指定索引的字符 | `"abc".charAt(0)` | `'a'` |
| `toCharArray()` | 变成字符数组 | `"abc".toCharArray()` | `['a','b','c']` |
| `substring(start, end)` | **截取** (含头不含尾) | `"abcdef".substring(0, 3)` | `"abc"` |
| `replace(old, new)` | 替换 (敏感词屏蔽) | `"TMD".replace("TMD", "***")` | `"***"` |
| `split(String regex)` | 切割 (常用于处理CSV) | `"a,b,c".split(",")` | `String[]{"a","b","c"}` |

-----

### 第二部分：String 实战案例 (视频 06-07)

#### 4\. 模拟用户登录 (Video 06)

  * **核心痛点**：新手最容易写 `if (name == "admin")`，这是错的！
  * **正确逻辑**：
    1.  已知正确的用户名密码。
    2.  键盘录入用户输入的。
    3.  使用 `equals()` 比较内容。

<!-- end list -->

```java
// 核心代码片段
String rightName = "itheima";
Scanner sc = new Scanner(System.in);
String inputName = sc.next();

// ❌ 错误：比较的是内存地址
if(inputName == rightName) { ... } 

// ✅ 正确：比较的是字符内容
if(inputName.equals(rightName)) { ... } 
```

#### 5\. 生成验证码 (Video 07)

  * **需求**：随机生成5位验证码，包含大写、小写、数字。
  * **思路分析**：
    1.  电脑不会“瞎想”，它只能从你给定的范围里选。
    2.  所以，先把所有可能的字符（A-Z, a-z, 0-9）拼成一个巨大的字符串 `datas`。
    3.  使用 `Random` 生成一个索引 `index`。
    4.  使用 `datas.charAt(index)` 拿到字符。
    5.  循环5次，拼接到一起。

<!-- end list -->

```java
// 简化版核心逻辑
String datas = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
String code = ""; // 用于拼接结果
Random r = new Random();

for (int i = 0; i < 5; i++) {
    int index = r.nextInt(datas.length()); // 随机生成索引
    char c = datas.charAt(index); // 拿到字符
    code += c; // 字符串拼接
}
System.out.println("验证码：" + code);
```

-----

### 第三部分：ArrayList 集合 (视频 08-10)

#### 6\. 为什么需要 ArrayList (Video 08)

  * **数组痛点**：`int[] arr = new int[3];` 一旦创建，长度就定死是3了。如果我想存第4个数据，就会报错。

  * **ArrayList**：它是**可变长**的容器。你只管往里 `add`，它底层会自动扩容（创建一个更大的数组并把数据搬过去）。

  * **泛型约束**：

      * `ArrayList<String>`：只能存字符串。
      * `ArrayList<Integer>`：只能存整数（**注意：不能写 `int`，必须写包装类 `Integer`**）。

#### 7\. ArrayList 常用方法 (Video 08)

记住这“增删改查”四板斧：

```java
ArrayList<String> list = new ArrayList<>();

// 1. 增 (Add)
list.add("Java");
list.add("Python"); // 此时 list: ["Java", "Python"]

// 2. 删 (Remove)
list.remove(0); // 删掉索引0，此时 list: ["Python"]
list.remove("Python"); // 删掉指定内容

// 3. 改 (Set)
list.set(0, "C++"); // 修改指定索引的内容

// 4. 查 (Get) - 重点！
String s = list.get(0); // 相当于数组的 arr[0]

// 5. 长度 (Size) - 重点！
int len = list.size(); // 相当于数组的 length
```

#### 8\. 综合应用案例 (Video 09-10)

视频里通常会让你做“学生管理”或者“查找特定元素”。

  * **场景**：创建一个集合，存入3个学生对象，然后把年龄低于18岁的学生找出来。
  * **代码实现**：

<!-- end list -->

```java
// 1. 定义集合，泛型是对象类型
ArrayList<Student> list = new ArrayList<>();

// 2. 添加对象 (假设 Student 类已经写好)
list.add(new Student("张三", 20));
list.add(new Student("李四", 16)); // 这个小于18
list.add(new Student("王五", 19));

// 3. 遍历集合
for (int i = 0; i < list.size(); i++) {
    // 4. 获取对象
    Student stu = list.get(i);
    
    // 5. 判断逻辑
    if (stu.getAge() < 18) {
        System.out.println("未成年人：" + stu.getName());
    }
}
```

-----

### 📝 总结

Day 08 的核心就两句话：

1.  **处理文本用 `String`**（注意比较要用 `equals`，内存有常量池）。
2.  **存储数量不确定的数据用 `ArrayList`**（注意存数字要用 `Integer`，获取长度用 `size()`）。

